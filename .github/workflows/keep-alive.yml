name: Keep Supabase Alive

on:
  schedule:
    - cron: '0 8 */6 * *'  # Every 6 days at 8am UTC
  workflow_dispatch:        # Allow manual trigger

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Supabase
        run: |
          curl -s \
            -H "apikey: sb_publishable_0Oexgft2ZmnOIl_mSHyzMg_6LDUBMwk" \
            -H "Authorization: Bearer sb_publishable_0Oexgft2ZmnOIl_mSHyzMg_6LDUBMwk" \
            "https://hepbnfdkmvrlbcsyrnmn.supabase.co/rest/v1/MBH%20-%20CRM?select=id&limit=1"
          echo "Supabase pinged successfully"
